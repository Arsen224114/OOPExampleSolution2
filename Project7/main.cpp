#include "Student.h"
#include "Manager.h"
#include "initializer.h"
#include "Worker.h"

int main () {
	//Human human
	//Student student;
	//1)Worker worker;
	//2)Worker* worker = new Worker;
	//3)Human* human = new Worker;
	//  Human* human = new Student;

	Human* human = new Human[4]{Student(), Worker(), }

	

	return 0;
}